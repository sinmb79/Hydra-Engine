# config/markets.yaml
markets:
  kr:
    enabled: true
    mode: paper
    exchange: kis
  us:
    enabled: true
    mode: paper
    exchange: kis
  upbit:
    enabled: false
    mode: paper
    exchange: upbit
  binance:
    enabled: true
    mode: paper       # 테스트넷 키 사용 중 — 실전 전환 시 live로 변경
    exchange: binance
  hl:
    enabled: true
    mode: paper       # Hyperliquid 테스트넷
    exchange: hyperliquid
  poly:
    enabled: false
    mode: paper
    exchange: polymarket
